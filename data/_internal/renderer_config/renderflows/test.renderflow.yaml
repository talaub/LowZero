resources:
  GBufferAlbedo:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferSurfaceNormals:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferNormals:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferMetalness:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: R8_UNORM
    array_size: 1
  GBufferRoughness:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: R8_UNORM
    array_size: 1
  GBufferDepth:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: D32_SFLOAT
    array_size: 1
    depth: true
  EntityIndexMap:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: R32_UINT
    array_size: 1
  DeferredLit:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  ShadowAtlas:
    dimensions:
      type: absolute
      x: 1024
      y: 1024
    type: image
    format: D32_SFLOAT
    array_size: 1
    depth: true
  IdWritebackBuffer:
    type: buffer
    array_size: 1
    buffer_size: 4
  SelectedIdBuffer:
    type: buffer
    array_size: 1
    buffer_size: 4
  HoverCoordinatesBuffer:
    type: buffer
    array_size: 1
    buffer_size: 8
  HoverHighlightImage:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
    
output_image: HoverHighlightImage
steps:
  - name: depthprepass
  - name: graph
  - name: ShadowPass
  - name: lighting
  - name: highlight_edge
  - name: debuggeometry
  - name: debuggeometry_nodepth
