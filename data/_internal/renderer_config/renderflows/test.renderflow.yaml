resources:
  GBufferAlbedo:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
steps:
  - name: test
  - name: graph
