resources:
  GBufferAlbedo:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferSurfaceNormals:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferNormals:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: RGBA8_UNORM
    array_size: 1
  GBufferMetalness:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: R8_UNORM
    array_size: 1
  GBufferRoughness:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: R8_UNORM
    array_size: 1
  GBufferDepth:
    dimensions:
      type: relative
      target: renderflow
      multiplier: 1.0
    type: image
    format: D32_SFLOAT
    array_size: 1
    depth: true
output_image: GBufferAlbedo
steps:
  - name: test
  - name: depthprepass
  - name: graph
